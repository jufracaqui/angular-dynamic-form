import { Component } from '@angular/core';

import { DynamicControl } from '../../models/dynamic-control';

@Component({
  selector: 'app-date',
  templateUrl: './date.component.html'
})
export class DateComponent extends DynamicControl { }
